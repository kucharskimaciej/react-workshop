extends ~layouts/columns

block title
    | Tasks

block content
    +slide_column()
        .l-column-fit
            +task('basic')
                | Replace `react-router` with the Redux version

            +task('basic')
                | Convert the state to use immutable objects

            +task('optional')
                | Add grouping contacts: multiple contact lists. A contact may belong to multiple groups.


